apiVersion: v1
kind: Service
metadata:
  name: guestbook-ui
spec:
  type: NodePort
  selector:
    app: guestbook-ui
  ports:
  - port: 80          # Service’in iç portu
    targetPort: 80     # Pod’un portu
    nodePort: 30091    # Node üzerindeki port (örnek olarak)
